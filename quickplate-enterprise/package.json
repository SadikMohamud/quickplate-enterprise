{
  "name": "quickplate-enterprise",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd packages/landing && npm run dev\" \"cd packages/customer-pwa && npm run dev\" \"cd packages/restaurant-admin && npm run dev\" \"cd packages/master-admin && npm run dev\"",
    "build": "npm run build --workspaces",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.2.2"
  }
}